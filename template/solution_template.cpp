#include <bits/stdc++.h>
#include "../utils/utils.h"
using namespace std;


int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    vector<string> lines;
    string s;
    while (getline(cin, s)) {
        lines.push_back(s);
    }

    // Part 1
    // TODO

    // Part 2
    // TODO

    return 0;
}

